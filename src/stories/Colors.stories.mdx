import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs'
import { themes } from '@/styles/themes'

<Meta title="Tema/Cores" />

<ColorPalette>
  {Object.entries(themes).map(([title, colors], index) => (
    <ColorItem
      key={index}
      title={title}
      colors={colors}
    />
  ))}
</ColorPalette>